{
  "name": "assembler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "组装器（TS）：需求→demo 代码（直接产出，代码即真相源）核心 + pi/local 驱动",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "pretest": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "serve": "npm run build && node dist/startServer.js"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@sinclair/typebox": "^0.34.52"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
