# Agent 是薄循环容器，零件外插

Agent 是一个只负责生命周期编排（循环、停止条件、返回）的薄容器组件，LLM、上下文、工具等零件从外部插入，不是被它黑盒包住。轨迹监测等横切能力挂在循环上。

未来读者会问"为什么 Agent 不是一个完整黑盒"——答案：零件外插才能做到组件级插拔（换 llm、换上下文策略不动 Agent 内部），并为多 agent 协作留位（多个 Agent 容器互相接线）。
